<!-- HEADER WITH PROMINENT, RESPONSIVE LOGO -->
<header class="site-header">
  <div class="logo">
    <img src="/assets/dovecote-logo.png" alt="Dovecote Estate" class="header-logo" />
  </div>

  <nav class="site-nav">
    <a class="contact-btn" href="/contact">Contact</a>
    <button class="menu-toggle" aria-label="Menu">☰</button>
  </nav>
</header>

<style>
  /* Layout */
  .site-header{
    position:relative;
    z-index:10;
    display:flex;
    align-items:center;
    justify-content:space-between;
    padding:0.5rem 1rem;
    background:transparent; /* overlay on hero image */
  }

  .logo{flex:0 0 auto; line-height:0;}

  /* --- MAKE LOGO PROMINENT --- 
     clamp(min, preferred, max) -> scales with viewport
     !important busts any framework max-height limits (Bootstrap, etc.) */
  .site-header .logo img.header-logo,
  .navbar .logo img.header-logo,
  img.header-logo{
    height: clamp(80px, 8vw, 160px) !important; /* visible on desktop, scales up/down */
    max-height: 22vh !important;               /* never exceed 22% of viewport height */
    width:auto !important;
    object-fit:contain;
    display:block;
    filter: drop-shadow(0 1px 4px rgba(0,0,0,.25)); /* subtle lift over photo */
  }

  /* Tweak nearby elements so they don't dwarf the logo */
  .contact-btn{
    font-size: .95rem;
    padding: .5rem .9rem;
    border:1px solid rgba(255,255,255,.7);
    border-radius: 999px;
    color:#fff; text-decoration:none;
    background:rgba(0,0,0,.25);
    backdrop-filter:saturate(120%) blur(2px);
  }
  .menu-toggle{ 
    font-size: 1.1rem; 
    background:transparent; border:0; color:#fff; margin-left:.5rem;
  }

  /* Tablet */
  @media (max-width:1024px){
    .site-header{ padding:.5rem .75rem; }
    .contact-btn{ font-size:.9rem; padding:.45rem .8rem; }
  }

  /* Mobile */
  @media (max-width:600px){
    .site-header{ padding:.4rem .6rem; }
    .site-header .logo img.header-logo{ height: clamp(56px, 12vw, 96px) !important; }
    .contact-btn{ font-size:.85rem; padding:.4rem .7rem; }
  }
</style>
