Prompt for Replit agent (copy-paste):

“Please replace my current header with the code below.
Use the uploaded logo file /assets/dovecote_estate_logo_TIGHT.png.
Remove the ‘Property’ and ‘Reservations’ links.
Keep only the Contact button (white background, black text, black oval border) and a hamburger for mobile.
Header must be sticky, full-width, minimal, with generous padding and a subtle shadow.

HTML (replace existing header markup)
<header class="site-header">
  <div class="header-inner">
    <a href="/" class="brand">
      <img
        src="/assets/dovecote_estate_logo_TIGHT.png"
        alt="Dovecote Estate Stanley"
        class="brand-logo"
        loading="eager"
        decoding="async"
      />
    </a>

    <a href="/contact" class="contact-btn">Contact</a>
    <button class="nav-toggle" aria-label="Open menu" aria-expanded="false">☰</button>
  </div>
</header>

CSS (add or replace)
:root {
  --header-bg: #fff;
  --text: #000;
  --container: 1200px;
  --pad-x: 32px;
  --pad-y: 18px;
  --shadow: 0 1px 2px rgba(0,0,0,.06);
}

/* Header shell */
.site-header {
  position: sticky;
  top: 0;
  z-index: 1000;
  background: var(--header-bg);
  box-shadow: var(--shadow);
}

/* Inner layout */
.header-inner {
  max-width: var(--container);
  margin: 0 auto;
  padding: var(--pad-y) var(--pad-x);
  display: flex;
  align-items: center;
  gap: 24px;
}

/* Logo */
.brand { display: flex; align-items: center; }
.brand-logo {
  display: block;
  height: 44px;      /* default size; increase if you want it bigger */
  width: auto;
  object-fit: contain;
}
@media (min-width: 1200px) {
  .brand-logo { height: 58px; } /* larger on desktop */
}

/* Contact button */
.contact-btn {
  margin-left: auto;            /* push to the right */
  background: #fff;
  color: #000;
  border: 2px solid #000;
  border-radius: 9999px;        /* pill shape */
  padding: 8px 20px;
  font-weight: 600;
  letter-spacing: .02em;
  text-decoration: none;
  transition: all .2s ease-in-out;
}
.contact-btn:hover { background: #000; color: #fff; }

/* Hamburger */
.nav-toggle {
  display: none;
  font-size: 24px;
  background: none;
  border: 0;
  color: #000;
  cursor: pointer;
  line-height: 1;
}
@media (max-width: 900px) {
  .contact-btn { display: none; }   /* hide button on small screens */
  .nav-toggle { display: block; margin-left: auto; }
}

/* Optional dropdown panel if we later add items for mobile */
body.nav-open .mobile-panel {
  display: block;
}
.mobile-panel {
  display: none;
  position: absolute;
  right: 20px;
  top: calc(100% + 8px);
  background: #fff;
  border: 1px solid #eee;
  border-radius: 10px;
  padding: 12px 16px;
  box-shadow: 0 8px 24px rgba(0,0,0,.08);
}

JS (add once, e.g., in header.js and import it)
const btn = document.querySelector('.nav-toggle');
if (btn) {
  btn.addEventListener('click', () => {
    const expanded = btn.getAttribute('aria-expanded') === 'true';
    btn.setAttribute('aria-expanded', String(!expanded));
    document.body.classList.toggle('nav-open', !expanded);
  });
}
